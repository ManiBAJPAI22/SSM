# deployment/docker-compose.yml
version: '3.8'

services:
  inference-server:
    build:
      context: ..
      dockerfile: deployment/Dockerfile.serving
    ports:
      - "50051:50051"
      - "8080:8080"
    volumes:
      - ../models:/app/models
    environment:
      - MODEL_PATH=/app/models/onnx/mamba_multimodal.onnx

  grpc-web-proxy:
    build:
      context: ..
      dockerfile: deployment/Dockerfile.grpc-web-proxy
    ports:
      - "8081:8081"
    depends_on:
      - inference-server
    environment:
      - GRPC_SERVER=inference-server:50051

  frontend:
    build:
      context: ..
      dockerfile: deployment/Dockerfile.frontend
    ports:
      - "3000:3000"
    depends_on:
      - grpc-web-proxy
    environment:
      - NEXT_PUBLIC_API_URL=http://grpc-web-proxy:8081

  prometheus:
    image: prom/prometheus:latest
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'

  grafana:
    image: grafana/grafana:latest
    ports:
      - "3001:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    volumes:
      - grafana-storage:/var/lib/grafana
      - ./grafana/dashboards:/etc/grafana/provisioning/dashboards
      - ./grafana/datasources:/etc/grafana/provisioning/datasources

volumes:
  grafana-storage:
